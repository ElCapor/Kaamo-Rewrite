// AEArray implementations moved into header (AEArray.h).
// This translation unit left intentionally minimal to avoid duplicate
// symbol definitions. The header provides the template implementations.
#include <abyss/AEArray.h>